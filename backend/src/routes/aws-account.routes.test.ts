import { describe,expect,it } from 'vitest';
import type { AwsAccountContext } from '../types.js';
import { existingAccountConflict } from './aws-account.routes.js';

const account=(overrides:Partial<AwsAccountContext>={}):AwsAccountContext=>({id:'internal-1',tenantId:'tenant-1',accountId:'143671530412',accountName:'Existing',accountType:'SANDBOX',environment:'sandbox',riskTier:'LOW',region:'af-south-1',connectionType:'LOCAL_DEFAULT_CREDENTIALS',connectionStatus:'PENDING',sourceType:'MANUAL',connectionSource:'MANUAL',hasConnection:true,isDemo:false,...overrides});
describe('AWS account conflict responses',()=>{it('returns recovery metadata for an existing pending account',()=>{const error=existingAccountConflict(account());expect(error).toMatchObject({status:409,code:'AWS_ACCOUNT_ALREADY_EXISTS',message:'This AWS account already exists for the active tenant.',details:{existingAccountId:'internal-1',connectionStatus:'PENDING',sourceType:'MANUAL',canValidate:true,canEdit:true,canRemove:true}})});it('identifies a demo collision as convertible rather than directly usable',()=>{expect(existingAccountConflict(account({sourceType:'DEMO_SEED',isDemo:true})).details).toMatchObject({sourceType:'DEMO_SEED',canValidate:false,canConvert:true})});it('offers connection repair when the manual account row has no connection',()=>{expect(existingAccountConflict(account({hasConnection:false,connectionSource:undefined})).details).toMatchObject({canRepair:true,canValidate:false})})});
